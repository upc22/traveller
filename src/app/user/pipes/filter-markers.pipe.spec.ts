import { FilterMarkersPipe } from './filter-markers.pipe';

describe('FilterMarkersPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMarkersPipe();
    expect(pipe).toBeTruthy();
  });
});
